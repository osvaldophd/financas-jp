// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`. 0dd93c6b8d5aed5e9495c45b70c3fe8

export const environment = {
  URLAPI: 'http://localhost/OFP/api/public/api',
  production: false,
  apiKey: '50dd93c6b8d5aed5e9495c45b70c3fe8',
  apiUrl: 'http://localhost/OFP/api/public/api',
  tokemAPI: "",
  API: 'http://localhost/OFP/api/public/api',
  IMGS: 'http://localhost/OFP/api/public/api'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
